import UsersSection from 'components/musiclyTheme/users/UserSection';

const Users: React.FC = () => {
	// if (loading) return <ActionLoader position="fixed" />;
	return <UsersSection />;
};

export default Users;
